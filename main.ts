// LightCode
console.log("Circut is loading")
light.setBrightness(25)
light.setPixelColor(0, color.rgb(255, 0, 0))
pause(150)
light.setPixelColor(1, color.rgb(255, 0, 0))
pause(150)
light.setPixelColor(2, color.rgb(255, 0, 0))
pause(150)
light.setPixelColor(3, color.rgb(255, 0, 0))
pause(150)
light.setPixelColor(4, color.rgb(255, 0, 0))
pause(150)
light.setPixelColor(5, color.rgb(255, 0, 0))
pause(150)
light.setPixelColor(6, color.rgb(255, 0, 0))
pause(150)
light.setPixelColor(7, color.rgb(255, 0, 0))
pause(150)
light.setPixelColor(8, color.rgb(255, 0, 0))
pause(150)
light.setPixelColor(9, color.rgb(255, 0, 0))
pause(500)
light.clear()
// OvenTempCode
while (true) {
    if (200 < input.temperature(TemperatureUnit.Fahrenheit) && input.temperature(TemperatureUnit.Fahrenheit) < 250) {
        light.setAll(color.rgb(40, 40, 0))
        pause(250)
        light.clear()
        light.setAll(color.rgb(40, 40, 0))
        pause(250)
        light.clear()
        console.log("The Temp is between 200 and 250")
    } else if (250 < input.temperature(TemperatureUnit.Fahrenheit) && input.temperature(TemperatureUnit.Fahrenheit) < 300) {
        light.setAll(color.rgb(65, 105, 225))
        pause(250)
        light.clear()
        light.setAll(color.rgb(65, 105, 225))
        pause(250)
        light.clear()
        console.log("The Temp is between 250 and 300")
    } else if (300 < input.temperature(TemperatureUnit.Fahrenheit) && input.temperature(TemperatureUnit.Fahrenheit) < 350) {
        light.setAll(color.rgb(127, 255, 0))
        pause(250)
        light.clear()
        light.setAll(color.rgb(127, 255, 0))
        pause(250)
        light.clear()
        console.log("The Temp is between 300 and 350")
    } else if (350 < input.temperature(TemperatureUnit.Fahrenheit) && input.temperature(TemperatureUnit.Fahrenheit) < 400) {
        light.setAll(color.rgb(153, 50, 204))
        pause(250)
        light.clear()
        light.setAll(color.rgb(153, 50, 204))
        pause(250)
        light.clear
        console.log("The Temp is between 350 and 400")
    } else if (400 < input.temperature(TemperatureUnit.Fahrenheit) && input.temperature(TemperatureUnit.Fahrenheit) < 450) {
        light.setAll(color.rgb(216, 112, 147))
        pause(250)
        light.clear()
        light.setAll(color.rgb(216, 112, 147))
        pause(250)
        light.clear()
        console.log("The Temp is between 400 and 450")
    } else if (450 < input.temperature(TemperatureUnit.Fahrenheit) && input.temperature(TemperatureUnit.Fahrenheit) < 500) {
        light.setAll(color.rgb(255, 250, 250))
        pause(250)
        light.clear()
        light.setAll(color.rgb(255, 250, 250))
        pause(250)
        light.clear()
        console.log("The Temp is between 450 and 500")
    }
    
}
